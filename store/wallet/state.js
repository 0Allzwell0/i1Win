const state = () => ({
    requestState: false,
    wallets: null,
    banksList: null,
    limits: null,
    balance: (0).toFixed(2),
    bonus: null,
    showPromptMsg: false
})

export default state